services:
  postgres:
    image: postgres:18
    ports:
      - '5433:5432'
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: trucklogistics
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
